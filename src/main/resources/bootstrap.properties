#This is application.properties file renamed as "bootstrap.properties" to let know limit service to use contact a spring config server to retrieve properties

spring.application.name=limits-service
spring.cloud.config.uri=http://localhost:8888
spring.profiles.active=dev
#management.endpoints.enabled-by-default=true

#server.port=8080

#These properties will be retrieved from spring cloud server
#limits-service.minimum=99
#limits-service.maximum=10000